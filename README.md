# Cat Diorama Lock

Black mobile lock-screen prototype using the local `sample (1).glb` model.

- Gyroscope support on mobile after tapping the compass button
- Pointer fallback on desktop
- Static files only, ready for GitHub Pages
- Meshopt-compressed GLB for faster mobile loading
