# Light Transplant (조명 이식)

**한 장의 스틸에 시각별 빛만 이식해서 하루를 만든다.**

생성 영상은 조명을 잘 만들지만 피사체를 가만두지 못한다. 그래서 영상에서 **빛만 떼어내고**
기하는 스틸 한 장에 고정한다. 두 부분으로 이루어진다.

| | 이름 | 하는 일 |
|---|---|---|
| 1 | **Frozen Reference** (기준 고정) | 스틸 한 장이 모든 디테일을 담당. 프레임을 절대 섞지 않는다 |
| 2 | **Sun Anchor** (해 앵커) | 클립 자체의 밝기 곡선을 실제 일출·남중·일몰에 맞춰 편다 |

씬 포맷 이름은 `renderMode: "fullday"`, 빌더는 `scripts/build_fullday.py`.
첫 적용: `assets/photo3d_nila_fullday/` (8초 192프레임 클립 → 96슬롯 = 15분 간격).

---

## 왜 이렇게 하는가 — 버려진 두 방식

### ① 영상을 런타임에 seek 한다 → 못 쓴다

가장 자연스러워 보이는 방법이고, 실제로는 하나도 신뢰할 수 없었다.

- DOM에 붙기 **전에** `src`를 세팅한 `<video>`는 Chrome에서 로드를 시작하지 않는다.
  `loadeddata`를 기다리던 로더가 에러 없이 통째로 멈춘다.
- 화면 밖 1×1 요소는 장식용으로 판정되어 `loadedmetadata`에서 멈춘다.
- 버퍼링 전에 seek을 걸면 `seeking` 상태에 눌러앉고 텍스처는 검은 채로 남는다.
- `python -m http.server`는 Range 요청을 무시한다(전체 파일을 200으로 응답). dev 서버에서는
  미디어 탐색 자체가 불가능하다.

빌드 타임에 프레임을 뽑으면 디코더·자동재생 정책·seek이 전부 사라진다.

### ② 인접 프레임을 크로스페이드한다 → 눈이 겹친다

부드러운 하루를 만들려면 당연해 보이지만, **생성 클립은 피사체를 가만두지 않는다.**
인접 슬롯 사이에서 고양이 머리가 평균 1.11px, p90 3.73px, 최대 6.67px 움직인다.
두 장을 겹쳐 그리면 눈이 두 개로 보인다. 실측:

| | 눈 영역 선명도 |
|---|---|
| 슬롯 77 한 장 | 232 |
| 슬롯 78 한 장 | 248 |
| **50/50 크로스페이드** | **121** |

간격이 벌어진 구간에서는 **눈이 네 개**로 나왔다. 영상을 다시 생성해도 해결되지 않는다.
생성기는 항상 조금씩 흔들리기 때문에, **두 프레임을 섞는다는 발상 자체가 틀렸다.**

---

## 방식

```
출력 = 기준프레임 × ( 그 시각의 빛 ÷ 기준프레임의 빛 )
```

- **기준프레임** — 클립에서 **가장 평평하게 조명된** 프레임 1장. 모든 픽셀 디테일을 담당한다.
- **라이트맵** — 각 슬롯 프레임을 σ=4로 뭉갠 것. 540×960으로 저장. 디테일은 사라지고
  그 시각의 빛(색·밝기 분포)만 남는다. **색은 전부 실제 영상에서 온 것이고 합성이 아니다.**

### 기준 프레임은 "가장 밝은"이 아니라 "가장 평평한"

처음엔 가장 밝고 중립적인 프레임을 골랐는데, 그게 하필 **햇빛 그림자가 가장 강한** 프레임이다.
기준에 구워진 그림자는 그게 없어야 할 모든 시각에서 **지워져야** 하는데, 저주파 비율로는
완전히 못 지운다. 결과: 새벽 2시 벽에 정오의 나뭇가지 그림자가 흐릿하게 탄 자국처럼 남는다.

실측 (벽 국소 그림자 대비 → 밤 시간대 벽 오차):

| 기준 프레임 | 그림자 대비 | 밤 벽오차 |
|---|---|---|
| 슬롯 23 (가장 밝음) | 25.84 | 1.72레벨 |
| **슬롯 87 (가장 평평)** | **6.00** | **1.04레벨** |

그래서 선택 기준은 **중립성 통과(최소채널 ≥ 60) 후 그림자 대비 최소**다. 없는 걸 더하는 건
쉽고 있는 걸 지우는 건 어렵다.

### 라이트맵 해상도는 저주파가 아니다

이름과 달리 라이트맵은 저주파가 아니다. 태양 그림자를 실어야 하고, 더 중요하게는 **기준에
구워진 그림자를 상쇄**할 수 있어야 한다. 90×160으로는 불가능하다.

| 라이트맵 | 벽 오차 (02:03) | 96장 |
|---|---|---|
| 90×160 | 4.37레벨 | 0.25MB |
| 270×480 | 2.71 | 0.96MB |
| **540×960** | **1.58** | **1.93MB** |

용량이 늘어서 슬롯 맵은 **씬 간 공유**한다(`view.json`의 `lightFrom`). 방의 빛은 피사체와
무관하므로 한 벌이면 된다 — 네 씬 합계 8.9MB → 3.8MB.

프레임을 섞는 일이 없으므로 **이중상이 구조적으로 불가능하다.** 검증: 슬롯 정중앙 시각과
슬롯 위 시각의 선명도 비 = **1.000** (흐려지는 구간이 존재하지 않음).

### 라이트맵에서 피사체를 반드시 뺀다

이게 두 번째 함정이다. 고양이는 컬러 벽 위의 밝은 흰 덩어리라, 프레임을 그냥 뭉개면
라이트맵이 **고양이 자신의 밝기 자국**을 품는다. 슬롯마다 원본 고양이 위치가 다르니 그
자국이 미끄러지고, **정지한 고양이 위로 실루엣이 떠다닌다.**

그래서 rembg로 피사체 영역(12개 슬롯의 합집합, 팽창)을 구해 빼고, 그 구멍을 주변 방의
빛으로 메운다. 구멍이 200px대라 **가중 블러로는 안 된다** — σ=12에서 가중치가 구멍 한가운데서
0으로 무너져 라이트맵이 검게 나온다. **피라미드(push-pull) 채움**을 쓴다.

검증: 네 시각의 고양이 픽셀이 **전역 색 스케일 하나로 1.1~2.6레벨** 안에서 설명된다.
순수하게 색만 변한다는 뜻이다.

### Sun Anchor — 클립을 실제 해에 맞춘다

이런 클립은 물리적 하루가 아니라 **색상환**이다. 그대로 틀면 가장 밝은 프레임이 05:30에,
가장 어두운 프레임이 16:45에 놓인다(오후가 이미 저녁 같다). 단순 회전으로는 부족하다 —
클립은 밝은 구간이 짧고 어두운 구간이 긴데 실제 낮은 반대이기 때문이다.

그래서 클립의 밝기 곡선을 직접 앵커한다.

| 클립의 특징점 | → 실제 시각 (기본값: 서울 8월 중순) |
|---|---|
| 가장 어두운 프레임 | 00:00 |
| 반밝기 교차 (상승) | 일출 05:50 |
| 가장 밝은 프레임 | 남중 12:35 |
| 반밝기 교차 (하강) | 일몰 19:20 |

사이는 늘려 채운다. 결과적으로 어두운 구간이 밤으로 압축되고 밝은 구간이 한낮에 펼쳐진다.
빌드 타임에 `timeToSlot` 테이블(10분 간격 145개, **언랩된** 값)로 구워 `view.json`에 넣고,
렌더러는 그걸 보간만 한다.

적용 후 실측:

| 시각 | 00 | 07 | 12 | 17 | 19 | 21 |
|---|---|---|---|---|---|---|
| 밝기 | 80 | 142 | **186** | 150 | 133 | 115 |
| 따뜻함 (R−B) | −107 | +53 | +54 | +102 | **+115** | +35 |

---

## 얻는 것

- **이중상 없음** — 구조적으로 불가능
- **15분 계단 없음** — 빛이 연속으로 보간된다
- **피사체가 안 움찔거린다** — 벽지로는 이게 맞다
- **에셋 5.1MB → 0.3MB** (기준 93KB + 라이트맵 161KB). 앱 번들에는 원본 프레임을 넣지 않는다
- 원본 대비 방(벽·아치) 재현 오차 **3.9~5.7레벨**

## 포기하는 것

- **그림자 모양이 고정된다.** 원본은 하루 동안 나뭇가지 그림자가 길어졌다 짧아졌다 하는데,
  릴라이트는 기준 프레임의 모양을 유지한 채 색만 바꾼다.
- **강한 색 구간에서 피사체가 과하게 물든다.** 파란 시간대에 원본 고양이는 흰색을 꽤 유지하는데
  릴라이트는 파랗게 물든다. 피사체 자리의 빛을 주변 벽에서 추정하기 때문.
  → 슬롯마다 원본에서 피사체 자신의 평균 색을 따로 측정해 적용하면 고칠 수 있다 (미구현).

---

## 피사체를 바꾸려면 — 스틸 **한 장**

라이트맵은 방의 빛만 담고 피사체는 빠져 있으므로, **96개 라이트맵과 시간 매핑은 그대로
재사용된다.** 바뀌는 건 기준 프레임 하나뿐이다.

**필요한 것: 같은 방 · 같은 카메라 구도에 새 피사체가 있는 스틸 1장.**

조건 세 가지:

1. **같은 방, 같은 카메라.** 라이트맵이 그 방에 공간적으로 정렬돼 있다. 아치·벽·바닥이
   어긋나면 빛이 엉뚱한 데 떨어진다. 기존 기준 프레임을 베이스로 피사체만 교체하는 방식이 가장 안전.
2. **조명은 중립적이고 밝게.** 기준의 조명은 나눗셈으로 제거되지만, 짙게 물든 스틸은 채널이
   뭉개져 되살릴 수 없다. 실측:

   | 기준 스틸의 조명 | 하루 평균 재현 오차 |
   |---|---|
   | 중립·밝음 | **3.4레벨** |
   | 올리브 톤 | 4.1레벨 |
   | 짙은 파랑 | **30.9레벨** ❌ |

3. **"첫 프레임"이 아니라 "기준 프레임"이다.** 빌더가 클립에서 가장 밝고 중립적인 프레임을
   자동으로 고른다(현재 슬롯 23).

```bash
python scripts/swap_reference.py <still.png> <source_scene> <new_scene_name>
```

스크립트가 하는 일: 스틸을 씬 프레임 크기로 맞추고 → **방·카메라 정렬을 검사해서 안 맞으면 거부** →
rembg로 새 피사체 영역 계산 → 그 스틸의 자체 라이트맵 계산 → 라이트맵 96장·`view.json`·시간 매핑을
그대로 복사 → 새 피사체로 타임라인 스트립 재생성.

### 적용 사례

| 씬 | 피사체 | 만든 방법 | 크기 |
|---|---|---|---|
| `nila_fullday` | 흰 먼치킨 | 8초 클립에서 빌드 | 0.3MB |
| `sage_fullday` | 아메리칸 숏헤어 (초록 모자) | **스틸 1장** + 위 라이트맵 재사용 | 0.39MB |

`sage_fullday`의 정렬 검사 결과: 시프트 (−0.1, +0.4)px, 아치 엣지 드리프트 0.3px — 같은 방, 같은 카메라.
클립을 새로 만들지 않고 하루 사이클을 그대로 얻었다.

---

## 만드는 법

```bash
# 클립 → 씬 (프레임 추출, 기준 선택, 라이트맵, 해 앵커, 타임라인 스트립)
python scripts/build_fullday.py samples/<clip>.mp4 <scene_name> 96

# 일출/남중/일몰을 바꾸려면
SUNRISE=5.5 SOLAR_NOON=12.3 SUNSET=19.7 python scripts/build_fullday.py ...

# 실루엣이 떠다니던 첫 버전을 재현하려면 (비교용)
LIGHT_INCLUDES_SUBJECT=1 python scripts/build_fullday.py ...
```

산출물 (`assets/photo3d_<name>/`):

| 파일 | 앱 번들 | 용도 |
|---|---|---|
| `reference.webp` | ✅ | 모든 디테일 |
| `light/l###.webp` × 96 + `light/ref.webp` | ✅ | 슬롯별 빛, 기준의 빛 |
| `view.json` | ✅ | `timeToSlot`, 슬롯 수, 해 시각 |
| `daystrip.webp` | ✅ | 스크럽 타임라인 (**시각 순서**) |
| `frames/f###.webp` × 96 | ❌ | QA 비교용 원본 |

## 확인 도구

- **스크럽 페이지** `fullday.html` — 타임라인 드래그, 하루 30초 재생, **원본 프레임 나란히 비교**
- **URL 노브** — `?tod=HH:MM` 시각 고정 · `?dayspeed=N` 배속 · `?scene=<name>`
- 호스트 페이지에서 `postMessage({type:"tod", value})`로 스크럽 (iframe 리로드 없이)

## 안드로이드

앱은 사이트를 APK에 번들한다(`file:///android_asset/viewer.html`). 새 씬은 세 군데를 고쳐야 한다.

1. `app/build.gradle`의 `syncWebWallpaperAssets` — `include "photo3d_<name>/**"`,
   그리고 `exclude "photo3d_<name>/frames/**"`
2. `MainActivity.java`의 `SCENES` 배열
3. `versionCode` / `versionName`

에셋 동기화는 반드시 `Sync` 태스크여야 한다. `Copy`는 include 목록에서 빠진 파일을 지우지
않아서, 폐기된 씬이 APK에 영원히 남는다.
